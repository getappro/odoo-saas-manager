<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- CRON: Check Server Health (Every 15 minutes) -->
        <record id="ir_cron_check_server_health" model="ir.cron">
            <field name="name">SaaS: Check Server Health</field>
            <field name="model_id" ref="model_saas_server"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_all_servers_health()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

        <!-- CRON: Check Subscription Expiry (Daily at 2:00 AM) -->
        <!--<record id="ir_cron_check_subscription_expiry" model="ir.cron">
            <field name="name">SaaS: Check Subscription Expiry</field>
            <field name="model_id" ref="model_saas_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_subscription_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=2, minute=0, second=0)"/>
        </record>-->

        <!-- CRON: Monitor Instances (Hourly) -->
        <record id="ir_cron_monitor_instances" model="ir.cron">
            <field name="name">SaaS: Monitor Instances</field>
            <field name="model_id" ref="model_saas_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_monitor_instances()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>

        <!-- CRON: Check User Limits (Daily at 3:00 AM) -->
        <record id="ir_cron_check_user_limits" model="ir.cron">
            <field name="name">SaaS: Check User Limits</field>
            <field name="model_id" ref="model_saas_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_user_limits()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=3, minute=0, second=0)"/>
        </record>

        <!-- CRON: Auto-Renew Subscriptions (Daily at 1:00 AM) -->
        <!--<record id="ir_cron_auto_renew_subscriptions" model="ir.cron">
            <field name="name">SaaS: Auto-Renew Subscriptions</field>
            <field name="model_id" ref="model_saas_subscription"/>
            <field name="state">code</field>
            <field name="code">model.cron_auto_renew()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=1, minute=0, second=0)"/>
        </record>-->

        <!-- CRON: Check Server Health (Hourly) -->
        <record id="cron_check_server_health" model="ir.cron">
            <field name="name">SaaS: Check Server Health</field>
            <field name="model_id" ref="model_saas_server"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_all_servers_health()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=1, minute=0, second=0)"/>
        </record>

        <!-- CRON: Sync user limits to all instances every hour -->
        <record id="cron_sync_user_limits" model="ir.cron">
            <field name="name">SaaS: Sync User Limits to Instances</field>
            <field name="model_id" ref="model_saas_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_all_user_limits()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <!--<field name="numbercall">-1</field>-->
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
